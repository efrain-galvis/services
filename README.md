# Services site

One-page site for Efrain Galvis AI consulting.

Live: https://efrain-galvis.github.io/services/

Open `index.html` in a browser. Copy lives in `index.html`. Styles in `css/styles.css`. Behavior in `js/main.js`.

No backend and no build step.

Contact: the page form opens a `mailto:` to `galvisefrain@gmail.com`. X (`https://x.com/EfrainGalvis1`) is an alternative.
